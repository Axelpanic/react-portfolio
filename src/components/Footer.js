import React from ('react');

function Footer ({currentPage, handlePageChange}) {
    return (
        <Footer>
            <p> Alex Hickey, written using MERN practicies, April 2023s</p>
        </Footer>
    )
};

export default Footer;